/* Properties */
var name = "David"
var age = 24

/* Declaration of a Function */
function printAge(nameUser, ageUser) {
  console.log(`${nameUser} tiene ${ageUser} años`)
}

printAge(name, age)
printAge("Andres", 28)
printAge("Vicky", 42)
printAge("Fernando", 10)
