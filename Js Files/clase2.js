/* Properties */
var name = "David"
var lastname = "Penagos"

/* Name with capital letter */
var nameCapitalLetter = name.toUpperCase()
/* Name with lower case */
var lastnameMinus = lastname.toLowerCase()
/* First letter of name */
var firstLetterOfName = name.charAt(0)
/* Length of name */
var numberLettersName = name.length

/* Way to make string interpolation */
var fullname = `${name} ${lastname.toUpperCase()}`

/* Way to make subtring of a String */
var str =  name.substr(1, 2)
